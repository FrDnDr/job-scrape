"""
preprocessing package — standalone data transformation scripts.

Modules:
    job_cleaner      — HTML stripping, whitespace normalisation, deduplication
    skill_normalizer — Alias resolution and canonical skill name mapping
    resume_parser    — Richer resume field extraction (education, roles, etc.)
    skill_extractor  — Catalog-driven skill detection with category awareness
    feature_generator — Structured feature vectors for AI matching context
"""
